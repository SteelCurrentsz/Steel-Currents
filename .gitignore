node_modules/
*.log
.DS_Store
.env
dist/
# Build outputs, all regenerable with `node build/standalone.mjs`.
# The build script itself is source and stays tracked.
build/bundle.js
build/steel-currents.html
build/*.png
# Throwaway probe pages: one ship on a plane of sea, so a model can be
# measured and photographed without starting a battle. Made as needed.
client/__*.html
www/
ios/
android/
*.jks
.gradle/
.idea/
*.xcodeproj/user/
DerivedData/
*.app/
*.ipa
*.apk
*.aab
